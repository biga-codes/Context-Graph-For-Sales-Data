# For local dev, keep this as /api if using Vite proxy.
# For Render static deployment, set this to your backend Render URL + /api
# Example: https://your-backend.onrender.com/api
VITE_API_BASE_URL=/api
